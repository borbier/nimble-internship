<h1>Groups Setting</h1>
<p>Let's change from <%= @group.name %> to </p><br />
<%= form_for ([@course, @group]) do |f|%>
  <%= label_tag(:name, "Name:") %>
  <%= f.text_field :name %>
  <button class="btn waves-effect waves-light">
    <%= f.submit "Create" %>
  </button>
<% end %>