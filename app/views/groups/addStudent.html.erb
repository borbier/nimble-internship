<h1>Groups#addStudent</h1>
<ul>
  <% @students.each do |student| %>
    <li>
      <%= link_to(student.first_name.concat(" ", student.last_name), course_group_updateAddStudent_path(:id => student.id), method: :post) %>
    </li>
  <% end %>
</ul>